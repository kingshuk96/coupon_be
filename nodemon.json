{
  "watch": ["src", "*"],
  "ext": "ts,js,json,yaml",
  "exec": "npm run lint && dotenv -e .env -- cross-env NODE_ENV=development ts-node --files -r tsconfig-paths/register src/server.ts | pino-pretty --colorize"
}
